This app requires `curl` and `jq` to be installed. They will be installed automatically as dependencies.

Weather data is fetched from Open-Meteo (free, no API key required).

Sports data is optional and requires a free API key from https://api-football.com
